---
import Layout from "../../layouts/Layout.astro";
import { microPosts } from "../../data/microPosts";
---

<Layout
  title="שאלות לוגיות קצרות"
  description="מיקרו-פוסטים של NeverMind: שאלות קצרות, תשובות חדות, וקישורים להעמקה כשצריך יותר ממשפט אחד."
>
  <section class="mx-auto max-w-5xl space-y-10 px-4 pb-20 text-right text-[#1A1A1A]">
    <header class="space-y-4">
      <p class="text-sm font-semibold uppercase tracking-[0.24em] text-[#D42B2B]">Micro-posts</p>
      <h1 class="text-[clamp(2.2rem,1.8rem+1.6vw,3.4rem)] font-semibold">שאלות לוגיות קצרות</h1>
      <p class="max-w-3xl text-[clamp(1rem,0.96rem+0.3vw,1.15rem)] leading-8 text-[#1A1A1A]/68">
        לא כל שאלה צריכה מאמר שלם. לפעמים מספיק פירוק אחד מדויק כדי לשנות כיוון, לנקות רעש, או להבין איפה בדיוק התחלת לבלבל בין עובדה לפירוש.
      </p>
    </header>

    <div class="grid gap-5">
      {microPosts.map((post) => (
        <article id={post.slug} class="rounded-[2rem] border border-[#1A1A1A]/10 bg-white/84 p-6 shadow-[0_18px_55px_rgba(26,26,26,0.05)]">
          <div class="space-y-3">
            <div class="flex flex-wrap items-center justify-between gap-3">
              <h2 class="text-[1.35rem] font-semibold text-[#1A1A1A]">{post.question}</h2>
              {post.tag && <span class="rounded-full border border-[#1A1A1A]/10 bg-[#FAFAF8] px-3 py-1 text-xs font-semibold text-[#1A1A1A]/62">{post.tag}</span>}
            </div>
            <p class="text-[1rem] leading-8 text-[#1A1A1A]/74">{post.answer}</p>
            {post.relatedHref && post.relatedLabel && (
              <a href={post.relatedHref} class="inline-flex text-sm font-semibold text-[#D42B2B] transition hover:opacity-75">
                {post.relatedLabel}
              </a>
            )}
          </div>
        </article>
      ))}
    </div>
  </section>
</Layout>
