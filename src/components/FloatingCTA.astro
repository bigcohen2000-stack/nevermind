---
interface Props {
  primaryHref: string;
  primaryLabel: string;
  secondaryHref?: string;
  secondaryLabel?: string;
}

const {
  primaryHref,
  primaryLabel,
  secondaryHref,
  secondaryLabel,
} = Astro.props;
---

<aside
  data-floating-cta
  class="pointer-events-none fixed inset-x-0 z-[65] px-4 opacity-0 translate-y-6 transition duration-300"
  style="bottom: max(0.75rem, env(safe-area-inset-bottom));"
>
  <div class="mx-auto flex max-w-3xl flex-col gap-4 rounded-[var(--nm-radius-md)] border border-[color-mix(in_srgb,var(--nm-fg)_10%,transparent)] bg-white/95 px-4 py-4 shadow-[var(--nm-shadow-lg)] backdrop-blur-xl sm:flex-row sm:items-center sm:justify-between">
    <div class="min-w-0 text-right">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[var(--nm-accent)]">פעולה מהירה</p>
      <p class="mt-1 text-sm leading-6 text-[color-mix(in_srgb,var(--nm-fg)_70%,var(--nm-bg))]">מכאן אפשר להמשיך בלי לחפש שוב את הכפתור המתאים.</p>
    </div>
    <div class="pointer-events-auto flex shrink-0 flex-col items-stretch gap-2 sm:flex-row sm:items-center">
      {secondaryHref && secondaryLabel && (
        <a
          href={secondaryHref}
          class="inline-flex min-h-[48px] w-full items-center justify-center rounded-full border border-[color-mix(in_srgb,var(--nm-fg)_10%,transparent)] bg-[var(--nm-surface-muted)] px-4 py-2 text-sm font-semibold text-[var(--nm-fg)] transition hover:bg-white sm:w-auto"
        >
          {secondaryLabel}
        </a>
      )}
      <a
        href={primaryHref}
        class="inline-flex min-h-[48px] w-full items-center justify-center rounded-full bg-[var(--nm-accent)] px-4 py-2 text-sm font-semibold text-[var(--nm-on-accent)] transition hover:bg-[var(--nm-accent-hover)] sm:w-auto"
      >
        {primaryLabel}
      </a>
    </div>
  </div>
</aside>

<script is:inline>
  document.addEventListener("astro:page-load", () => {
    const floatingCta = document.querySelector("[data-floating-cta]");
    if (!(floatingCta instanceof HTMLElement)) return;

    const updateVisibility = () => {
      const scrollableHeight = document.documentElement.scrollHeight - window.innerHeight;
      const progress = scrollableHeight > 0 ? window.scrollY / scrollableHeight : 0;
      const isVisible = progress >= 0.6;
      floatingCta.classList.toggle("pointer-events-none", !isVisible);
      floatingCta.classList.toggle("opacity-0", !isVisible);
      floatingCta.classList.toggle("translate-y-6", !isVisible);
      floatingCta.classList.toggle("opacity-100", isVisible);
      floatingCta.classList.toggle("translate-y-0", isVisible);
    };

    window.addEventListener("scroll", updateVisibility, { passive: true });
    updateVisibility();
  });
</script>
