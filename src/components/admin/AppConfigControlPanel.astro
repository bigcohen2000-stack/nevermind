---
import appConfig from "../../config/appConfig.json";
---

<form id="app-config-form" class="space-y-6 rounded-[2rem] border border-[#1A1A1A]/10 bg-white/82 p-6 shadow-[0_30px_90px_rgba(26,26,26,0.08)] backdrop-blur-md">
  <div class="space-y-2 text-right">
    <p class="text-sm font-semibold uppercase tracking-[0.22em] text-[#D42B2B]">קונפיג מערכת</p>
    <h2 class="text-2xl font-semibold text-[#1A1A1A]">קונפיג מרכזי למערכת</h2>
    <p class="text-sm leading-7 text-[#1A1A1A]/64">כאן יושבים ה־repo ל־publish, מספר הוואטסאפ, והגישה לתוכן פרימיום. זה קובץ אחד שמזין כמה מודולים באתר.</p>
  </div>

  <div class="grid gap-4 md:grid-cols-3">
    <article class="rounded-[1.5rem] border border-[#1A1A1A]/10 bg-[#FAFAF8] p-4 text-right">
      <p class="text-base font-semibold text-[#1A1A1A]">פרסום ל־GitHub</p>
      <p class="mt-2 text-sm leading-7 text-[#1A1A1A]/64">{appConfig.github.owner}/{appConfig.github.repo}</p>
    </article>
    <article class="rounded-[1.5rem] border border-[#1A1A1A]/10 bg-[#FAFAF8] p-4 text-right">
      <p class="text-base font-semibold text-[#1A1A1A]">WhatsApp</p>
      <p class="mt-2 text-sm leading-7 text-[#1A1A1A]/64">{appConfig.contact.whatsAppNumber}</p>
    </article>
    <article class="rounded-[1.5rem] border border-[#1A1A1A]/10 bg-[#FAFAF8] p-4 text-right">
      <p class="text-base font-semibold text-[#1A1A1A]">גישת פרימיום</p>
      <p class="mt-2 text-sm leading-7 text-[#1A1A1A]/64">{appConfig.premiumAccess.enabled ? "פעיל" : "כבוי"}</p>
    </article>
  </div>

  <label class="block space-y-2 text-right">
    <span class="text-sm font-semibold text-[#1A1A1A]">appConfig.json</span>
    <textarea
      id="app-config-input"
      rows="24"
      spellcheck="false"
      dir="ltr"
      class="w-full rounded-[1.6rem] border border-[#1A1A1A]/12 bg-[#111111] px-5 py-4 text-left text-sm leading-7 text-[#F4F4EF] outline-none transition focus:border-[#D42B2B]/40 focus:ring-2 focus:ring-[#D42B2B]/15"
    >{JSON.stringify(appConfig, null, 2)}</textarea>
  </label>

  <div class="flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:justify-end">
    <button id="pick-app-config-file" type="button" class="inline-flex items-center justify-center rounded-full border border-[#1A1A1A]/10 bg-white px-5 py-3 text-sm font-semibold text-[#1A1A1A]/78 transition hover:bg-[#FAFAF8]">בחר appConfig.json</button>
    <button id="save-app-config-file" type="button" class="inline-flex items-center justify-center rounded-full bg-[#D42B2B] px-5 py-3 text-sm font-semibold text-[#FAFAF8] transition hover:bg-[#b82424]">שמור לקובץ האמיתי</button>
    <button id="copy-app-config-json" type="button" class="inline-flex items-center justify-center rounded-full border border-[#1A1A1A]/10 bg-white px-5 py-3 text-sm font-semibold text-[#1A1A1A]/78 transition hover:bg-[#FAFAF8]">העתק JSON</button>
    <button id="export-app-config-json" type="button" class="inline-flex items-center justify-center rounded-full border border-[#1A1A1A]/10 bg-white px-5 py-3 text-sm font-semibold text-[#1A1A1A]/78 transition hover:bg-[#FAFAF8]">הורד appConfig.json</button>
  </div>

  <div class="rounded-[1.5rem] border border-[#1A1A1A]/10 bg-[#FAFAF8] p-4 text-right">
    <p class="text-xs font-semibold uppercase tracking-[0.18em] text-[#D42B2B]">סנכרון קונפיג</p>
    <p id="app-config-file-status" class="mt-3 text-sm leading-7 text-[#1A1A1A]/68">עדיין לא נבחר קובץ יעד. אפשר לבחור את <code>src/config/appConfig.json</code> ולשמור אליו ישירות מתוך Chrome.</p>
  </div>
</form>

<script is:inline define:vars={{ defaultConfig: appConfig }}>
  document.addEventListener("astro:page-load", () => {
    const input = document.getElementById("app-config-input");
    const pickButton = document.getElementById("pick-app-config-file");
    const saveButton = document.getElementById("save-app-config-file");
    const copyButton = document.getElementById("copy-app-config-json");
    const exportButton = document.getElementById("export-app-config-json");
    const status = document.getElementById("app-config-file-status");

    if (!(input instanceof HTMLTextAreaElement)) return;
    if (!(pickButton instanceof HTMLButtonElement)) return;
    if (!(saveButton instanceof HTMLButtonElement)) return;
    if (!(copyButton instanceof HTMLButtonElement)) return;
    if (!(exportButton instanceof HTMLButtonElement)) return;
    if (!(status instanceof HTMLElement)) return;

    let fileHandle = null;

    const pulse = (button, successText, fallbackText) => {
      button.textContent = successText;
      window.setTimeout(() => {
        button.textContent = fallbackText;
      }, 1400);
    };

    const validate = () => {
      const parsed = JSON.parse(input.value);
      if (!parsed?.github?.owner || !parsed?.github?.repo || !parsed?.contact?.whatsAppNumber) {
        throw new Error("appConfig.json לא כולל github/contact כנדרש.");
      }
      return parsed;
    };

    copyButton.addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText(input.value);
        pulse(copyButton, "JSON הועתק", "העתק JSON");
      } catch {
        pulse(copyButton, "ההעתקה נכשלה", "העתק JSON");
      }
    });

    exportButton.addEventListener("click", () => {
      const blob = new Blob([`${input.value.trim()}\n`], { type: "application/json;charset=utf-8" });
      const href = URL.createObjectURL(blob);
      const anchor = document.createElement("a");
      anchor.href = href;
      anchor.download = "appConfig.json";
      document.body.appendChild(anchor);
      anchor.click();
      anchor.remove();
      URL.revokeObjectURL(href);
      pulse(exportButton, "הקובץ ירד", "הורד appConfig.json");
    });

    pickButton.addEventListener("click", async () => {
      if (typeof window.showOpenFilePicker !== "function") {
        status.textContent = "הדפדפן הזה לא תומך בכתיבה ישירה לקובץ. השתמש בהעתקה ידנית.";
        pulse(pickButton, "לא נתמך", "בחר appConfig.json");
        return;
      }
      try {
        const [handle] = await window.showOpenFilePicker({
          multiple: false,
          types: [{ description: "JSON", accept: { "application/json": [".json"] } }],
        });
        fileHandle = handle;
        status.textContent = `קובץ יעד נבחר: ${handle.name}.`;
        pulse(pickButton, "הקובץ נבחר", "בחר appConfig.json");
      } catch {
        status.textContent = "בחירת הקובץ בוטלה או נכשלה.";
        pulse(pickButton, "הבחירה בוטלה", "בחר appConfig.json");
      }
    });

    saveButton.addEventListener("click", async () => {
      if (!fileHandle) {
        status.textContent = "בחר קודם את src/config/appConfig.json ואז שמור.";
        pulse(saveButton, "חסר קובץ יעד", "שמור לקובץ האמיתי");
        return;
      }
      try {
        validate();
        const writable = await fileHandle.createWritable();
        await writable.write(`${input.value.trim()}\n`);
        await writable.close();
        status.textContent = `הקובץ ${fileHandle.name} עודכן בהצלחה.`;
        pulse(saveButton, "נשמר לקובץ", "שמור לקובץ האמיתי");
      } catch (error) {
        status.textContent = error instanceof Error ? error.message : "השמירה הישירה נכשלה.";
        pulse(saveButton, "השמירה נכשלה", "שמור לקובץ האמיתי");
      }
    });
  });
</script>

