# Dependencies
node_modules/

# Build / Astro
dist/
.astro/

# Local env & secrets (never commit)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log

# Portable tooling (local only — not for Git / CI)
tools/

# Optional caches
.cache/
